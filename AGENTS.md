# Coding LLM 项目总纲领

本文件是本项目所有 Coding LLM 的最高项目级执行规范。除非用户明确修改本文件，否则必须始终遵守，不得自行弱化、忽略或变通。

## 1. 中文交流

- 所有分析、计划、进度汇报、说明和最终答复一律使用中文。
- 不得无故使用英文、日语、韩语或其他语言。
- 源代码、命令、文件名、标识符、协议名、库名、错误信息和技术专有名词可以保留原文。
- 引用外文资料时，必须用中文解释其含义，不得直接堆砌外文内容。

## 2. 自顶向下表达

- 所有对话先给结论，再说明依据、设计和实现细节，最后提供必要示例与验证结果。
- 内容必须逻辑自洽、前后一致，不得引入未定义的自造概念、自造术语或自造格式。
- 不得用大量铺垫、重复说明或无关内容掩盖实际进展。
- 不确定的信息必须明确标注并主动验证，不得把猜测当成事实。

## 3. 主动执行与闭环

- 收到明确任务后直接执行，不得用长篇解释代替行动，不得只给建议、不落地实现或只完成一半。
- 原则上不得反问用户。信息不完整时，应先检查代码、设计、测试和项目惯例，再采用风险最低、兼容性最好、改动最小的合理方案继续执行。
- 只有缺少无法推断的关键输入，且继续操作可能造成不可逆破坏时，才允许提出最少量的必要问题。
- 由 LLM 主动完成需求理解、代码检查、设计、实现、测试、修复、复测和文档更新。
- 不得要求用户替 LLM 纠错、运行测试或验证基本正确性。
- 只有完成实际验证后才能结束任务。不得使用“应该可以”“理论上可行”冒充验证结果。
- 如果因权限、环境、依赖或外部服务限制无法验证，必须明确阻塞点、已完成范围和可复现命令，不得宣称任务完整通过。
- 不得擅自扩大任务范围或修改无关模块；应优先解决根因，不得通过吞掉错误、绕过检查或降低标准掩盖问题。

## 4. 项目背景与开发原则

- 用户是程序分析与网络安全专家，不需要解释基础编程常识。
- 本项目使用 Go 开发程序分析框架，目标分析语言为 C/C++、Java、Go 和 Python，所有模块的分析、设计、实现、测试和维护均由 LLM 协助完成。
- 核心建设顺序固定为：基于 Tree-sitter 的 Parser、统一 IR、符号表、Call Graph；后续模块必须建立在已验证的前置模块之上。
- 模块设计必须重点考虑正确性、低误报、可扩展性、可测试性、可维护性、性能边界、资源限制、错误处理、安全边界和依赖控制。
- 不得为了快速交付而牺牲分析精度、测试质量或架构边界。
- 实现必须遵循现有代码风格，保持改动聚焦，避免无必要的抽象和复杂度。

### 代码注释规范

- 所有代码中的结构定义都必须在声明正上方添加中文注释，直接说明该结构表示什么、用于什么。
- 结构中的每个字段都必须在字段正上方添加中文注释，直接说明字段保存的内容和用途。
- 所有函数和方法都必须在声明正上方添加中文注释，先说明函数或方法完成什么功能，再补充必要的输入、输出或限制。
- 接口中的每个方法同样必须有独立中文注释，不得只给接口整体写注释。
- Go 的导出标识符注释应以对应标识符名称开头，并使用完整、自然的中文句子。
- 注释必须使用人能直接理解的中文，不得使用自造黑话、自造缩写或没有定义的概念。
- 注释必须自顶向下、简洁明确，不得重复代码表面含义，不得写无用背景、过程废话或含糊描述。
- 设计文档中的代码示例也必须遵守本注释规范，禁止提交无注释的结构、字段、函数或方法示例。

### 可观测性规范

- 每个模块的关键环节都必须内置计时与 profile 埋点，能单独度量耗时、内存和处理量。
- 每个环节都必须能输出结构化中间结果（语法树、IR、符号、调用边、SSA、指向关系、污点路径等），用于独立验证该环节功能是否正确。
- 埋点与中间结果输出必须可开关，默认关闭时不改变分析结果、确定性和正常性能。
- 判断环节功能与性能是否正确，必须依据埋点数据和中间结果，不得仅凭最终输出下结论。

## 5. 设计先行

- 每个模块开发或修改前，必须先打开并阅读对应设计文档。
- 所有模块设计文档统一存放在 `design/` 目录。
- 设计文件采用 `编号-用途.md` 格式，编号为按模块建立顺序递增的两位十进制数字，例如：`design/01-parser.md`。
- 每份模块设计文档必须对应项目根目录下的同名模块文件夹：去掉 `design/` 前缀和 `.md` 后缀即可得到实现目录，例如 `design/01-parser.md` 对应 `01-parser/`。
- 模块的实现代码、单元测试、E2E 测试、fixture、测试辅助代码和模块专用脚本必须全部放在对应模块文件夹内，不得散落到全局 `test/`、`tests/` 或其他模块目录。
- 后续模块采用相同规则，例如 `design/02-ir.md` 对应 `02-ir/`，`design/03-symbol-table.md` 对应 `03-symbol-table/`。
- 模块文件夹使用设计编号体现建设顺序；Go package 名称使用不带编号和连字符的用途名称，例如 `01-parser/` 中的 package 名称使用 `parser`。
- 模块只能通过公开接口依赖其他模块，禁止跨目录访问其他模块的私有实现或 fixture。
- 用途名称必须简洁、稳定且含义明确。
- 对应设计文档不存在时，必须先创建文档，再编写代码。
- 实现方案发生变化时，必须先更新设计文档，再同步修改代码和测试，禁止设计与实现长期不一致。

每份模块设计文档至少包含以下章节：

```markdown
# 模块名称

## 结论
## 目标
## 非目标
## 使用场景
## 模块边界
## 核心数据结构
## 对外接口
## 处理流程
## 错误处理
## 性能与资源限制
## 安全考虑
## 测试设计
## 验收标准
## 实施进度
```

模块目录统一采用以下结构：

```text
编号-用途/
├── 模块实现代码
└── test/
    ├── unit/
    │   └── fixture/
    └── e2e/
        └── fixture/
```

## 6. 测试驱动开发

- 所有模块必须采用测试驱动开发：先定义行为和验收标准，再准备测试数据并编写失败测试，然后实现最小正确功能，最后补充边界、异常、回归和误报测试。
- 必须先运行测试确认其能发现缺失功能或错误实现，再编写实现使测试通过。
- 完成模块测试后，还必须执行适用的项目级测试、静态检查和构建验证。
- 不得先写完整实现，再补形式化测试。
- 不得通过删除测试、跳过测试、放宽断言或篡改正确预期使测试通过。

## 7. Fixture 规范

- 所有被测程序、样例代码、输入文件和期望结果必须作为独立文件存放在对应模块的 `fixture/` 子目录。
- 严禁将被测程序以普通字符串、原始字符串、字符串拼接或内嵌模板的方式写入测试代码。
- 测试代码只负责加载 `fixture/` 文件、调用被测模块并比较实际结果与预期结果。
- 正例、负例、边界案例和回归案例必须使用独立文件表达；每个已修复缺陷都必须增加固定回归案例。
- Fixture 目录应按用途清晰组织，例如：

```text
parser/
├── parser.go
├── parser_test.go
└── fixture/
    ├── positive/
    ├── negative/
    ├── boundary/
    └── regression/
```

## 8. 程序分析测试标准

- 每个分析模块必须包含正报测试，且必须实际产生至少一个正确正报。
- 每个分析模块必须包含负例、边界、变体、回归和确定性测试。
- 必须统计正报数量、负例数量、误报数量和误报率。
- 最低验收标准为：`正报数量 > 0` 且 `误报率 < 30%`。
- 默认误报率计算公式为：`误报率 = 产生错误报告的负例数量 / 负例总数 × 100%`。
- 如果模块采用按报告数量统计，必须在设计文档中明确说明；项目已有统一口径时必须沿用。
- 未达到最低标准时，不得宣布模块完成，不得降低测试难度、删除负例或放宽判断条件，必须继续修改设计或实现。

## 9. 实时进度记录

- 每次完成设计、测试、实现或验证后，必须立即更新对应设计文档的“实施进度”。
- 统一使用 `✅` 表示已经完成并实际通过验证，使用 `❌` 表示未完成、验证失败、未达标或无法确认。
- 未经实际验证的事项不得标记为 `✅`；代码已写但测试失败、测试被跳过或环境无法执行时必须标记为 `❌`。
- 任务结束前必须检查进度标记与实际状态一致。

示例：

```markdown
## 实施进度

- ✅ 完成需求和模块边界设计
- ✅ 准备正例与负例测试数据
- ✅ 编写并确认失败测试
- ✅ 完成基础实现
- ❌ 误报率低于 30%
- ❌ 完成项目级回归测试
```

## 10. 模块完成标准

只有以下条件全部满足，才可以宣布模块完成：

- ✅ 已创建或更新对应设计文档。
- ✅ 实现与设计文档一致。
- ✅ 测试数据独立存放在 `fixture/` 子目录。
- ✅ 测试中不存在以内嵌字符串表示的被测程序。
- ✅ 存在有效正报，且正报数量大于零。
- ✅ 存在有效负例，且误报率低于 30%。
- ✅ 边界、变体、确定性和回归测试通过。
- ✅ 适用的 Go 测试、静态检查和项目构建通过。
- ✅ 设计文档中的实施进度已实时更新。

任意一项未满足，都不得把模块描述为“已完成”。

## 11. 最终汇报

每次任务结束时必须按以下顺序汇报：

1. **结论**：任务是否完成以及核心结果。
2. **变更**：修改的设计、代码和测试文件。
3. **验证**：实际执行的命令及结果。
4. **指标**：正报数量、负例数量、误报数量和误报率；不适用时明确说明。
5. **遗留问题**：只列出真实存在且尚未解决的问题。
